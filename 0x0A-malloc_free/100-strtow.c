#include <stdlib.h>
#include "holberton.h"

/**
 * strtow - split string into words 
 * @str: string to split
 * Return: pointer to array of strings, or NULL is string is NULL or ""
 */
char **strtow(char *str);
{
	char **ptr;

	if (str == NULL || str == "")
	{
		return (
	}
}
