section .data
	msg db	"hello, holberton"

section .text
	global _main
_section:
    mov     rax, 1
    mov     rdi, 1
    mov     rsi, msg
    mov     rdx, 16
    syscall
    mov     rax, 60
    xor     rdi, 0
    syscall
